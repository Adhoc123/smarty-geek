import React from 'react';
import { LineChart } from 'recharts';

const Single = ({quiz}) => {
    const {name, total} = quiz
    //console.log(quiz)
    return (
        <div>
           
        </div>
    );
};

export default Single;